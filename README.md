# Omarchy Community Knowledge — data ledger

Local, unpublished bootstrap. There are no real community records yet.
Canonical data will be append-mostly JSON, with case/change/report/event types.
Schemas and all executable tooling live in the sibling toolkit repository.

Do not add private journals, raw machine inventories, credentials or generated
test fixtures here. Publication of sanitized seeds requires an exact preview and
explicit approval. The data repository will have GitHub Actions disabled.
